import { Routes } from '@angular/router';

export const routes: Routes = [];
import { CreateFormElements } from './pages/create-form-elements/create-form-elements.page';

export const CREATE_ELEMENTS_FORM_ROUTES: Routes = [
  {
    path: '',
    component: CreateFormElements
  }
];
